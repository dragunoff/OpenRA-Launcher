{
  "name": "OpenRA Launcher Flutter",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter",
        "Google.arb-editor",
        "sst-dev.opencode"
      ],
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash",
        "dart.flutterSdkPath": "/usr/lib/flutter"
      }
    }
  },
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "postCreateCommand": "flutter pub get && dart run build_runner build && flutter gen-l10n",
  "remoteUser": "vscode",
  "forwardPorts": [8081, 5037]
}
